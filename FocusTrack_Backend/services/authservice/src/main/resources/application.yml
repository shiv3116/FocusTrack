server:
  port: 8081

spring:
  application:
    name: authservice
  datasource:
    url: jdbc:postgresql://localhost:5432/focustrackdb
    username: postgres
    password: admin123#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
